@functions {
    public string IsActive(string page)
    {
        var currentPage = ViewContext.RouteData.Values["page"]?.ToString();
        return string.Equals(currentPage, page, StringComparison.OrdinalIgnoreCase) ? "active" : "";
    }
}
<!DOCTYPE html>
<html lang="pt-pt">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Luxury Cars</title>

    <link rel="stylesheet" href="~/css/global/layout.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/global/styles.css" asp-append-version="true" />

    @* Toastr para notificações do botão de favoritos *@
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css" />

    @await RenderSectionAsync("Styles", required: false)

    @* Scripts essenciais - ATUALIZADO PARA V7 *@
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/tsparticles@2.12.0/tsparticles.bundle.min.js"></script>

    <style>
        #tsparticles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background-color: #050505;
        }

        body, .luxury-theme, .main-content, .container {
            background: transparent !important;
        }

        header {
            position: relative;
            z-index: 10;
        }
    </style>
</head>
<body class="luxury-theme">

    <div id="tsparticles"></div>

    <nav class="navigation">
        <ul>
            <li class="list @IsActive("/Index")">
                <b></b><b></b>
                <a asp-page="/Index">
                    <span class="icon"><ion-icon name="compass-outline"></ion-icon></span>
                    <span class="title">Explorar</span>
                </a>
            </li>

            @if (User.Identity.IsAuthenticated)
            {
                <li class="list @IsActive("/dashboard")">
                    <b></b><b></b>
                    <a asp-page="/dashboard">
                        <span class="icon"><ion-icon name="speedometer-outline"></ion-icon></span>
                        <span class="title">Dashboard</span>
                    </a>
                </li>
                <li class="list @IsActive("/Anunciar")">
                    <b></b><b></b>
                    <a asp-page="/Anunciar">
                        <span class="icon"><ion-icon name="add-circle-outline"></ion-icon></span>
                        <span class="title">Vender Carro</span>
                    </a>
                </li>
                <li class="list @IsActive("/Favoritos")">
                    <b></b><b></b>
                    <a asp-page="/Favoritos">
                        <span class="icon"><ion-icon name="diamond-outline"></ion-icon></span>
                        <span class="title">Favoritos</span>
                    </a>
                </li>
                <li class="list @IsActive("/Messages/Index")">
                    <b></b><b></b>
                    <a asp-page="/Messages/Index">
                        <span class="icon"><ion-icon name="chatbubbles-outline"></ion-icon></span>
                        <span class="title">Mensagens</span>
                    </a>
                </li>

                @if (User.IsInRole("Admin"))
                {
                    <li class="list @IsActive("/Admin/Aprovacoes")">
                        <b></b><b></b>
                        <a asp-page="/Admin/Aprovacoes">
                            <span class="icon"><ion-icon name="checkmark-done-circle-outline"></ion-icon></span>
                            <span class="title">Aprovações</span>
                        </a>
                    </li>
                }
            }
        </ul>

        <ul class="nav-bottom" style="margin-bottom: 20px;">
            <li class="list @IsActive("/auth")">
                <b></b><b></b>
                <a asp-page="/auth">
                    <span class="icon"><ion-icon name="person-outline"></ion-icon></span>
                    <span class="title">@(User.Identity.IsAuthenticated ? "O Meu Perfil" : "Entrar")</span>
                </a>
            </li>

            @if (User.Identity.IsAuthenticated)
            {
                <li class="list">
                    <b></b><b></b>
                    <a asp-page="/Logout">
                        <span class="icon"><ion-icon name="log-out-outline"></ion-icon></span>
                        <span class="title">Sair</span>
                    </a>
                </li>
            }
        </ul>
    </nav>

    <div class="main-content">
        <header style="padding: 20px; text-align: right; display: flex; justify-content: flex-end; align-items: center; gap: 10px;">
            @if (User.Identity.IsAuthenticated)
            {
                <div style="display: flex; flex-direction: column; align-items: flex-end;">
                    <span style="color: #d4af37; font-weight: 500;">Olá, @User.Identity.Name</span>
                    @if (User.IsInRole("Admin"))
                    {
                        <small style="color: #ff4d4d; font-size: 0.7em; text-transform: uppercase;">Administrador</small>
                    }
                </div>
                <div class="user-avatar" style="width: 35px; height: 35px; background: #d4af37; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: #000;">
                    <ion-icon name="person"></ion-icon>
                </div>
            }
        </header>

        <main role="main" class="container" style="padding: 20px;">
            @RenderBody()
        </main>
    </div>

    @* Scripts de terceiros *@
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>

    @* Configuração tsParticles *@
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            tsParticles.load("tsparticles", {
                fpsLimit: 60,
                particles: {
                    number: { value: 12, density: { enable: true, area: 800 } },
                    color: { value: "#ffffff" },
                    shape: { type: "circle" },
                    opacity: {
                        value: { min: 0.05, max: 0.15 },
                        animation: { enable: true, speed: 0.5, sync: false }
                    },
                    size: { value: { min: 150, max: 400 } },
                    move: {
                        enable: true,
                        speed: 0.8,
                        direction: "top-right",
                        random: true,
                        outModes: { default: "out" }
                    },
                    shadow: { enable: true, blur: 50, color: "#ffffff" }
                },
                detectRetina: true
            });
        });
    </script>

    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>